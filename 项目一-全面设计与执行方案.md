# 项目一：宠物数字纪念网站——全面设计与执行方案

> 工作名称：数字小窝  
> 文档状态：执行基线 v1.1（核心 MVP 已落地）  
> 编制日期：2026-09-04  
> 预计工期：单人全职约 25–30 个工作日，不含主体认证、备案和支付审核等待时间  
> 产品基线：[项目确认文档](./项目一-宠物数字纪念网站-项目确认文档.md)  
> 原始立项：[十个产品立项文档](./十个产品立项文档.md#项目一宠物-ai-纪念网站主线)

---

## 0. 如何使用这份文档

这是一份可以直接开工的产品、体验、视觉、技术和项目执行基线。开发时按第 23 节的顺序推进，每个阶段只有在验收条件全部满足后，才进入下一阶段。

当需求发生变化时：

1. 先把变化记录到“变更记录”；
2. 判断属于 MVP、后续版本还是明确不做；
3. 写清对工期、数据和测试的影响；
4. 再修改代码，禁止在开发中静默扩展范围。

### 0.1 2026-09-04 实施状态

本轮新增：人工定制现已具备不扣款申请、材料/负责人/截止日/修改轮次的运营工单、超过两轮的超范围提示，以及用户确认交付完成的流程；用户联系方式仅向本人和拥有运营角色的账户开放，运营更新需要填写审计原因。

公开纪念页的访问会按“纪念页 + HMAC-SHA-256 匿名指纹 + 当日”去重；管理台可以查看当日/近 7 日访问、近 7 日创建和近 30 日支付汇总，且不保存原始 IP。生产环境必须由 `VISITOR_FINGERPRINT_SECRET` 提供独立密钥。

已完成并通过自动化验证的当前闭环：Vue 响应式站点、邮箱注册/登录、服务端会话、受控图片上传、可写照片说明并调整顺序的照片相册、纪念页私密草稿、时间线增删改、写给 TA 的信、带乐观锁、自动保存与离开未保存内容提醒的可视化编辑器、暖阳／星夜／花园三套可保存且在公开页生效的主题、三个明确标注为演示内容的完整样板页、主动发布、公开页、已发布页面的一键复制分享链接与 PNG 分享卡片、由 Java 生成的安全 Open Graph 分享元数据、亲友留言和每日一次的访客点亮；留言可举报并由页面主人隐藏或恢复显示，命中基础高风险规则的留言会先进入待审核状态；账户可下载 ZIP 资料包，并可在二次确认后提交删除请求：公开纪念页和图片会立即下线，用户可在 7 天内撤销，期满后后台清理账号与内容。发布时由后端校验封面与至少 3 张照片。发布范围已实现“仅自己、持链接、口令、公开”四档；草稿图片不会因知道地址而被公开读取，口令页和图片会在同一浏览器会话解锁后访问。页面主人可以安全地下线并恢复分享，原内容和链接不会删除。套餐有服务端价格/权益快照、账户订单和权益页；本地模拟支付在事务中更新订单并发放权益，重复回调不会重复发放；免费或已生效套餐的照片与时间线容量由后端实际校验。公开页的口令、留言、点亮和举报操作具备单实例访客限流；点亮记录以纪念页、脱敏访客标识和服务端日期去重，生产多实例部署时需切换为 Redis 等共享限流器。管理台现已由服务端 `ADMIN`/`MODERATOR` 角色强制保护，可审核留言并巡查公开纪念页；下线页面必须填写原因、立即关闭公开访问并写入审计记录，普通用户直接调用接口会被拒绝。前端生产构建、Java 打包、单元测试及使用内存数据库的完整 API 流程均已通过；本机已通过显式 `local` H2 配置完成注册、上传、创建、编辑、退出和未登录拦截的真实浏览器联通验收；同域 Docker 部署编排与版本化 PostgreSQL 迁移已准备完成。

补充：本地模拟付款现在支持由 `ADMIN`/`MODERATOR` 填写原因后撤销；订单将标为已退款，对应权益同步撤销，并保留审计记录。它不等同于真实支付退款，生产仍必须使用支付渠道接口及经复核的退款规则。

以下内容仍属于后续 Sprint，不能表述为“已上线”：短信验证码、对象存储/MinIO 迁移、平台级内容审核、微信支付及商户审核、备案与监控告警。站内已提供内容规范和支付开放前说明，但真实支付和最终退款规则仍需要用户提供合规的商户/服务商配置，并由实际运营主体复核后才能接入或生效。

本版本锁定以下决策：

| 决策 | 结论 |
|---|---|
| 产品定位 | 为离世宠物建立温柔、私密、可分享、可长期托管和导出的数字纪念空间 |
| 用户侧主张 | 主打陪伴和记忆，不主打 AI，不使用“复活”表述 |
| 首发平台 | 移动端网站，适配微信内置浏览器和桌面端 |
| 前端 | Vue 3 + TypeScript + Vite |
| 后端 | Java 21 + Spring Boot，REST API |
| 架构 | 前后端分离、同域部署、后端模块化单体 |
| 数据 | PostgreSQL + Spring JDBC + Flyway |
| 登录 | 首个可用版本采用邮箱和密码；短信验证码在具备合规服务商后接入 |
| 会话 | Spring Security 服务端会话 + HttpOnly Cookie，不在 localStorage 保存长期身份令牌 |
| 文件 | 首个可用版本使用 Java 本地持久卷；对象存储直传在后续 Sprint 迁移 |
| 支付 | ¥0 / ¥99 / ¥299 / ¥699 起；定制服务现可提交不扣款申请，真实微信支付待商户配置后接入 |
| 发布隐私 | 默认“仅链接可见”，用户主动选择公开索引或密码访问 |
| 首发边界 | 不做数字分身、声音克隆、陌生人社区、广告和月订阅 |

---

## 1. 产品目标

### 1.1 用户问题

宠物离世后，照片和故事散落在手机、朋友圈与网盘中。用户需要的不是另一个文件夹，而是一处能够完整表达陪伴、方便亲友共同访问、又不会过度消费悲伤的纪念空间。

### 1.2 一句话价值

> 上传照片和故事，把关于 TA 的一生整理成一座随时可以回来看看、也能与亲友分享的数字小窝。

### 1.3 MVP 业务目标

- 跑通“访问 → 创建 → 预览 → 登录 → 发布 → 分享 → 付费”的完整闭环；
- 让普通用户在 10 分钟内发布一个基础纪念页；
- 支持平台安全保存、展示、导出和删除用户内容；
- 验证 ¥99 自助套餐与 ¥699 人工定制是否存在真实付费需求；
- 建立能够继续扩展主题、容量、实体纪念物和渠道合作的技术底座。

### 1.4 成功指标

| 指标 | MVP 目标 | 统计窗口 |
|---|---:|---|
| 首页到开始创建转化率 | ≥12% | 公测后 30 天 |
| 开始创建到成功发布率 | ≥35% | 公测后 30 天 |
| 发布后 24 小时内分享率 | ≥50% | 公测后 30 天 |
| 已发布用户付费转化率 | ≥5% | 公测后 30 天 |
| 创建流程中位完成时间 | ≤10 分钟 | 首批 30 位用户 |
| 页面访问成功率 | ≥99.5% | 连续 7 天 |
| P0/P1 未关闭缺陷 | 0 | 上线门禁 |
| 数据导出与删除成功率 | 100% | 上线门禁 |

北极星指标：**每周被至少一位亲友访问的有效纪念页数量。**

### 1.5 非目标

- 不建设宠物社交社区；
- 不提供医疗、心理或殡葬咨询；
- 不生成宠物数字人或模拟宠物对话；
- 不承诺无法保证的“永久存在”；
- 不开发原生 App 和小程序；
- 不拆微服务，不引入消息队列和分布式事务；
- 不在 MVP 中自动生成复杂视频或纪念册实物订单。

---

## 2. 用户与核心场景

### 2.1 首要用户

最近失去宠物、希望整理照片和故事的 20–40 岁养宠人。其主要阻力不是不会使用工具，而是情绪重、材料多、难以开始。

### 2.2 次要用户

- 帮伴侣、父母或朋友制作纪念页的人；
- 希望提前记录宠物成长过程的主人；
- 宠物摄影师、医院或善终机构推荐来的客户。

### 2.3 用户任务

| 场景 | 用户想完成的事情 | 产品回应 |
|---|---|---|
| 不知道从哪里开始 | 先做出一个有感觉的第一屏 | 三步完成昵称、封面、纪念语并即时预览 |
| 照片太多 | 快速上传、排序和分组 | 批量上传、进度显示、拖拽排序、封面选择 |
| 不知道写什么 | 获得结构化提示 | 提供问题引导和可跳过模板，不虚构内容 |
| 想与家人共同纪念 | 把页面分享给指定亲友 | 仅链接可见、密码访问、留言和点亮 |
| 担心隐私 | 知道谁能看到、能否删除 | 默认非公开、清晰权限、随时导出和删除 |
| 没精力自己制作 | 有人替自己整理 | 固定范围、固定价格的人工定制服务 |

---

## 3. 核心用户流程

### 3.1 创建与发布

```text
首页
  ↓ 点击“免费为 TA 建小窝”
输入昵称、封面和一句纪念语
  ↓
即时生成首屏预览
  ↓
上传照片 → 补充时间线 → 写给 TA 的信
  ↓
选择主题与隐私方式
  ↓
登录/绑定手机号并保存草稿
  ↓
完整预览
  ↓
免费发布或选择套餐
  ↓
获得访问链接与分享卡片
```

创建前允许用户完成首屏预览。未登录草稿只保存在浏览器本地，不上传照片到服务器；开始上传照片前必须登录，避免匿名垃圾文件与隐私风险。

### 3.2 访客纪念

```text
打开分享链接
  ↓
根据访问设置直接进入或输入密码
  ↓
浏览封面、故事、时间线和照片
  ↓
点亮蜡烛 / 送花 / 写留言
  ↓
留言通过风控后展示
  ↓
可再次分享，不出现付费弹窗
```

### 3.3 购买升级

```text
预览受限功能或进入价格页
  ↓
选择套餐
  ↓
后端创建待支付订单并锁定价格快照
  ↓
调起支付
  ↓
支付平台异步回调
  ↓
后端验签和幂等处理
  ↓
订单变为已支付并发放套餐权益
  ↓
前端刷新权益并展示凭证
```

支付成功以服务端回调为准，前端跳转结果仅用于提示，不直接发放权益。

### 3.4 删除与导出

```text
用户中心 → 数据与隐私
  ├─ 导出：生成 ZIP → 限时下载地址 → 记录完成时间
  └─ 删除：二次验证 → 页面立即下线 → 7 天撤销期 → 到期后硬删除
```

---

## 4. 信息架构与路由

### 4.1 前台路由

| 路由 | 页面 | 登录要求 | 索引策略 |
|---|---|---|---|
| `/` | 首页 | 否 | index |
| `/examples` | 纪念样板列表 | 否 | index |
| `/examples/:slug` | 授权或演示样板 | 否 | index |
| `/pricing` | 套餐价格 | 否 | index |
| `/help` | 帮助中心 | 否 | index |
| `/legal/privacy` | 隐私政策 | 否 | index |
| `/legal/terms` | 用户协议 | 否 | index |
| `/legal/content` | 内容规范 | 否 | index |
| `/create` | 创建向导 | 照片上传前要求登录 | noindex |
| `/editor/:id` | 纪念页编辑器 | 所有者 | noindex |
| `/preview/:id` | 所有者预览 | 所有者 | noindex |
| `/m/:slug` | 正式纪念页 | 取决于隐私设置 | 默认 noindex |
| `/login` | 登录 | 否 | noindex |
| `/account` | 用户中心 | 是 | noindex |
| `/account/orders` | 订单列表 | 是 | noindex |
| `/account/privacy` | 数据与隐私 | 是 | noindex |

### 4.2 管理后台路由

| 路由 | 功能 |
|---|---|
| `/admin` | 运营概览与异常提醒 |
| `/admin/memorials` | 纪念页搜索、查看、隐藏和恢复 |
| `/admin/tributes` | 留言审核、删除和申诉处理 |
| `/admin/reports` | 举报工单 |
| `/admin/orders` | 订单、退款和权益 |
| `/admin/services` | 人工定制任务 |
| `/admin/users` | 用户查询与账号状态 |
| `/admin/audit` | 管理操作日志 |

---

## 5. 页面与交互设计

### 5.1 首页

页面顺序：

1. 顶部导航：Logo、纪念样板、价格、帮助、登录；
2. 首屏：主标题、副标题、免费创建按钮、样板按钮和一张高质量纪念页预览；
3. 三步说明：上传记忆、整理故事、邀请亲友；
4. 纪念样板：三张主题卡片，可进入完整页面；
5. 信任说明：默认私密、可导出、无页面广告、不会用于训练；
6. 套餐摘要：免费、守护、珍藏、定制；
7. 常见问题：能否删除、谁能看、保存多久、能否修改；
8. 页脚：协议、隐私、备案信息、客服入口。

首屏文案：

> 给 TA 留一个随时可以回去看看的小窝

> 上传照片、写下故事、邀请亲友留言。几分钟创建一座温柔、私密、可长期托管的宠物数字纪念空间。

主按钮：**免费为 TA 建小窝**  
次按钮：**先看看纪念样板**

验收要求：

- 390px 宽度下首屏不横向滚动；
- 主按钮在首屏可见；
- 最大内容绘制时间目标不超过 2.5 秒；
- 没有自动播放声音、强制弹窗或倒计时促销。

### 5.2 创建向导

采用顶部步骤指示器，步骤为：

1. 认识 TA：昵称、类型、陪伴日期；
2. 第一张照片：封面上传和裁切；
3. 一句话：提供三个问题提示，可跳过；
4. 首屏预览：先让用户看到成果；
5. 登录保存：验证码登录后进入完整编辑器。

交互规则：

- 每个步骤最多一个主要动作；
- 必填项只包含昵称和封面；
- 日期允许只填年份、只填月份或选择“不确定”；
- 用户返回上一步时保留全部输入；
- 登录失败不清空已填写内容；
- 浏览器本地草稿保留 24 小时并明确提示。

### 5.3 纪念页编辑器

已实现：桌面端采用左侧模块列表、中间编辑区、右侧实时预览；移动端采用编辑与预览两个标签页，并通过键盘可操作的标签和锚点模块导航切换内容。

模块：

- 封面；
- 关于 TA；
- 陪伴时间线；
- 照片和短视频；
- 写给 TA 的信；
- 亲友纪念区；
- 主题、音乐与隐私。

必须提供：

- 2 秒防抖自动保存；
- “保存中 / 已保存 / 保存失败”状态；
- 模块拖拽排序；
- 图片上传真实进度、失败项单独重试和删除确认；
- 离开页面时的未保存提醒；
- 发布前检查清单；
- 套餐限制旁显示原因和升级入口，但不阻断基础编辑。

### 5.4 正式纪念页

默认结构：

1. 全屏或大幅封面；
2. 昵称、陪伴日期和一句纪念语；
3. 关于 TA；
4. 时间线；
5. 照片故事；
6. 写给 TA 的信；
7. 亲友留言与点亮；
8. 页面所有者设置的署名与平台说明。

页面以“TA 怎样生活过”为叙事中心，不把离世日期放成最醒目的元素。

### 5.5 价格页

价格页同时展示价格、容量、托管期限、退款边界与可导出能力，不使用隐藏附加费用。

| 套餐 | 价格 | 主要权益 |
|---|---:|---|
| 免费体验 | ¥0 | 1 页、12 张照片、5 个时间线节点、三套主题、基础留言、完整导出 |
| 温暖守护 | ¥99/次 | 80 张照片、3 个短视频、30 个时间线节点、三套主题、密码访问、分享卡片、3 年托管权益 |
| 时光珍藏 | ¥299/次 | 300 张照片、15 个短视频、100 个时间线节点、三套主题、密码访问、分享卡片、10 年托管权益 |
| 专属定制 | ¥699 起/次 | 人工整理、故事润色、页面代建、2 轮修改、7 个工作日交付、含时光珍藏权益 |

默认突出 ¥99 套餐，不用“最划算”诱导，改用“适合大多数完整纪念页”。

### 5.6 登录与用户中心

- 手机验证码登录；
- 验证码发送后 60 秒可重发；
- 单个手机号和 IP 需限流；
- 用户中心展示草稿、已发布、已隐藏和待删除页面；
- 提供订单、套餐权益、导出、删除账号和客服联系入口；
- 管理员入口不在普通导航中展示。

### 5.7 管理后台

MVP 只做运营必需能力：

- 今日新增用户、草稿、发布、支付和举报数量；
- 按昵称、slug、用户手机号后四位搜索；
- 隐藏违规纪念页、删除违规留言、恢复误处理内容；
- 查询订单、重新发放权益、发起退款；
- 所有管理操作记录操作者、时间、目标和原因。

---

## 6. 视觉设计系统

### 6.1 创意方向

关键词：**温暖、安静、真实、像一本被珍藏的相册。**

避免黑金殡葬风、廉价渐变、科技蓝和大量 AI 发光效果。页面让照片成为主角，装饰只承担层次与情绪作用。

### 6.2 基础色板

```css
:root {
  --color-canvas: #F7F2EA;
  --color-surface: #FFFDFC;
  --color-text: #2B2926;
  --color-text-muted: #665F58;
  --color-primary: #8A4F3D;
  --color-primary-hover: #713D2F;
  --color-sage: #6F8063;
  --color-night: #273248;
  --color-border: #DED6CB;
  --color-danger: #B43A3A;
  --color-success: #4F7654;
  --color-focus: #315EAF;
}
```

正文与背景的对比度必须达到 WCAG AA；焦点、错误和成功状态不能只依赖颜色表达。

### 6.3 字体

- 标题：思源宋体或系统中文衬线字体，强调纪念册气质；
- 正文：思源黑体或系统无衬线字体，保证移动端阅读；
- 数字和控制组件统一使用无衬线字体；
- 正文字号不低于 16px，正文行高 1.7；
- 长文内容宽度控制在 42–48 个中文字符。

字体必须本地托管或使用系统字体栈，不能依赖中国大陆访问不稳定的境外字体 CDN。

### 6.4 间距与布局

- 基础间距单位：4px；常用间距：8、12、16、24、32、48、64；
- 首页最大宽度：1200px；
- 长文最大宽度：760px；
- 卡片圆角：16px；输入框圆角：12px；
- 移动端页面左右安全间距：16px；
- 桌面端断点：768px、1024px、1280px。

### 6.5 阴影与质感

- 只使用低对比度、宽扩散阴影；
- 纸张纹理透明度不超过 4%，不能影响照片和文字；
- 不在所有卡片上加边框与阴影，优先通过留白分组；
- 图片保持自然比例，避免统一强裁切导致宠物主体被截断。

### 6.6 动效

- 页面进入：180–260ms 淡入与轻微上移；
- 卡片悬停：150ms，位移不超过 2px；
- 时间线出现：按阅读顺序逐项显示；

### 6.7 前端改版约定

- 全局颜色、字体和阴影仅在 `web/src/assets/design-tokens.css` 调整；页面样式使用其 CSS 变量，不把新的品牌色直接散落到组件中；
- 品牌名、导航与定制服务状态文案维护在 `web/src/config/site.ts`；套餐卡与套餐页文案维护在 `web/src/config/pricing.ts`；
- 改版优先调整上述配置和设计令牌，再调整对应页面的布局组件；接口字段和支付判断仍由 Java 服务端决定，不能因改版转入前端。
- 支持 `prefers-reduced-motion`，关闭非必要动画；
- 禁止自动轮播、闪烁、粒子爆炸和高频视差。

### 6.7 MVP 三套主题

| 主题 | 视觉 | 适合内容 |
|---|---|---|
| 暖阳相册 | 米白、陶土、纸张与自然光 | 通用猫狗纪念 |
| 星夜陪伴 | 深蓝、柔和星点、低亮度金色 | 安静、克制的长文纪念 |
| 小小花园 | 鼠尾草绿、植物线稿、浅色背景 | 小型宠物和轻盈照片 |

三套主题共享同一内容结构，只替换设计令牌和装饰资源，不能复制三份页面逻辑。

### 6.8 无障碍要求

- 所有交互元素支持键盘操作和清晰焦点环；
- 图片上传必须填写替代文本，可默认使用“宠物昵称 + 照片序号”并允许修改；
- 表单错误同时提供文字说明；
- 按钮点击区域至少 44×44px；
- 音乐默认暂停，并提供明确的播放、暂停和音量控制；
- 弹窗打开后锁定焦点，关闭后回到触发按钮。

---

## 7. 内容设计

### 7.1 文案原则

- 使用“TA、陪伴、故事、记得、小窝”；
- 不使用“复活、重生、再见一面”等误导性表达；
- 不承诺“治愈悲伤”；
- AI 润色前提示用户，结果必须由用户确认；
- 不生成用户没有提供过的回忆和事实。

### 7.2 编辑提示问题

- 你第一次见到 TA 是什么时候？
- TA 最有代表性的小习惯是什么？
- 哪一个普通瞬间最让你想念？
- 如果现在能说一句话，你想告诉 TA 什么？

这些问题只用于帮助开始写作，全部可跳过。

### 7.3 空状态

| 场景 | 文案 |
|---|---|
| 还没有时间线 | “从第一次相遇开始，记下一件关于 TA 的小事。” |
| 还没有留言 | “这里还很安静。亲友的第一句话会出现在这里。” |
| 上传失败 | “这张照片还没有传好。你可以重试，已经填写的内容不会丢失。” |
| 自动保存失败 | “暂时无法保存。请保持页面打开，我们会继续重试。” |
| 页面已隐藏 | “这座小窝目前只有主人可以访问。” |

### 7.4 内容安全提示

公开页面不得发布他人联系方式、身份证件、医疗记录等无关敏感信息。上传家庭合照前，用户需确认拥有使用权或已获得相关人员同意。

---

## 8. 功能需求与验收标准

### 8.1 P0：身份与会话

用户故事：作为创建者，我希望安全登录并在再次访问时找回自己的纪念页。

验收标准：

- [ ] 正确验证码可创建或登录用户；
- [x] 错误密码连续失败后触发限流；
- [x] 登录或注册成功后服务端更新会话 ID，防止会话固定；
- [x] Cookie 设置 HttpOnly、Secure 和 SameSite；
- [x] 退出登录立即使服务端会话失效；
- [x] 普通用户无法访问其他用户的草稿、订单和定制服务申请；
- [x] 管理员权限通过角色校验，不依赖前端隐藏按钮。

### 8.2 P0：纪念页草稿

- [x] 创建草稿后返回唯一 ID；
- [x] 仅所有者可查看与修改；
- [x] 昵称和封面为发布前必填；
- [x] 日期允许未知和部分精度；
- [x] 自动保存失败可重试且不覆盖较新的版本；
- [x] 同时编辑发生版本冲突时提示刷新，不静默覆盖。

### 8.3 P0：媒体上传

- [ ] Java 校验用户、纪念页归属、套餐容量和 MIME 类型后签发上传凭证；
- [ ] 前端直传对象存储并显示真实进度；
- [ ] 上传完成后必须通知 Java 确认文件；
- [ ] 后端核对文件大小、签名和归属后将媒体设为可用；
- [x] 图片删除后立即从纪念页移除并撤销读取权限，进入 24 小时延迟物理删除队列；
- [ ] 原图默认私有，公开纪念页只使用受控派生图或签名地址；
- [ ] 上传失败可重试，不产生重复媒体记录。

MVP 允许：JPEG、PNG、WebP；视频支持 MP4。HEIC 在浏览器不兼容时提示用户转换，首版不在服务端自动转换。

### 8.4 P0：时间线与纪念信

- [x] 时间线节点支持日期、标题、正文、相册图片关联与手工排序；
- [x] 日期可精确到年、月或日；
- [x] 纪念信支持纯文本换行，不接受任意 HTML；
- [x] 所有用户文本输出时转义，防止 XSS；
- [x] 长文本超过限制时在提交前提示，不直接截断。

### 8.5 P0：发布与隐私

- [x] 发布前检查昵称、封面和至少 3 张照片；
- [x] 隐私选项为仅链接可见、密码访问和公开索引；
- [x] 默认选择仅链接可见；
- [x] 密码使用强哈希保存，数据库不存明文；
- [x] 隐藏页面后原访问链接立即不可浏览；
- [x] slug 不使用手机号、昵称拼音或连续数据库 ID；
- [x] 所有者修改已发布页面后保留最近发布时间。

### 8.6 P0：访客留言与点亮

- [x] 点亮行为按访客标识与时间窗口限流；
- [x] 留言需要昵称和正文，不要求访客注册；
- [x] 留言提交前展示内容规范；
- [x] 命中基础高风险规则的留言进入待审核，不立即公开；
- [x] 页面所有者可隐藏自己页面上的留言；
- [x] 访客可举报留言；
- [x] 后台操作必须记录原因。
- [x] 运营角色可巡查公开纪念页，并在填写原因后立即下线；页面主人可自行恢复分享。

### 8.7 P0：套餐、订单与权益

- [x] 订单保存套餐、价格、币种和权益快照；
- [x] 相同幂等键不会创建两笔有效订单；
- [ ] 支付回调必须验签并可重复接收；
- [x] 订单支付成功只发放一次权益；
- [x] 本地模拟退款会在同一事务中更新订单、撤销权益并写入运营审计记录；真实支付退款仍需商户渠道、退款规则和合规复核后接入；
- [x] 免费版达到限制时仍可浏览和导出已有内容；
- [ ] 不因套餐到期删除内容，先进入只读和续期提醒流程。

### 8.8 P0：数据导出与删除

- [x] 用户完成二次验证后可发起导出；
- [x] ZIP 包含结构化 JSON、可读 Markdown 和原始媒体清单；
- [x] 下载链接短期有效且只能由本人获取；
- [x] 删除申请后纪念页与公开图片立即从访客侧下线；
- [x] 7 天内允许撤销，到期后由后台任务以原子到期核验完成数据库与本地存储硬删除；
- [ ] 支付与法定留存记录按适用规则最小化保存，与展示数据隔离。

### 8.9 P1：人工定制

- [x] 用户可以提交定制需求和联系方式；
- [x] 运营人员可记录材料是否齐全、负责人、截止日期和修改轮次；
- [x] 超过两轮修改必须显示为超范围；
- [x] 用户确认交付后服务任务完成。

### 8.10 P2：后续能力

- 多人共同编辑；
- 周年提醒；
- PDF 纪念册；
- 实体二维码纪念牌；
- 合作机构推荐码和分成；
- 经合规评估后的 AI 文字辅助。

---

## 9. 系统架构

### 9.1 总体架构

```text
┌──────────────────────────────────────────────┐
│ 浏览器 / 微信内置浏览器                       │
│ Vue 3 SPA：创建、编辑、账户、后台、纪念页交互 │
└──────────────────────┬───────────────────────┘
                       │ HTTPS / JSON / Cookie
┌──────────────────────▼───────────────────────┐
│ Nginx                                        │
│ 静态资源 /assets/**   API /api/**   页面 /m/**│
└───────────┬──────────────────┬───────────────┘
            │                  │
            │                  ▼
            │     ┌────────────────────────────┐
            │     │ Spring Boot 模块化单体      │
            │     │ user / memorial / media    │
            │     │ tribute / billing / admin  │
            │     └───────┬─────────┬──────────┘
            │             │         │
            ▼             ▼         ▼
       Vue 静态文件   PostgreSQL  对象存储/CDN
                                  │
                                  ├─ 微信支付
                                  ├─ 短信服务
                                  └─ 内容审核服务
```

### 9.2 架构选择

采用模块化单体：所有后端模块构建为一个 Spring Boot 应用，但代码和数据库访问按照业务模块隔离。

原因：

- 团队规模小，微服务会增加部署、日志、事务和联调成本；
- 订单、权益和纪念页需要本地事务一致性；
- 当前访问量没有独立扩容某个服务的证据；
- 模块边界清晰后，未来有真实压力时仍可拆分。

代价：所有后端模块一起发布；单个进程故障会影响全部业务。MVP 通过健康检查、备份和快速回滚解决，不提前引入分布式复杂度。

### 9.3 后端模块边界

| 模块 | 负责 | 不负责 |
|---|---|---|
| `user` | 登录、会话、角色、同意记录 | 纪念页业务 |
| `memorial` | 宠物、纪念页、主题、时间线、纪念信、发布 | 文件实际存储、支付 |
| `media` | 上传授权、文件元数据、访问地址、删除 | 纪念页内容编排 |
| `tribute` | 留言、点亮、举报 | 用户套餐 |
| `billing` | 套餐、订单、支付、退款、权益 | 纪念页编辑 |
| `moderation` | 审核案件、处置状态、申诉 | 登录认证 |
| `service` | 人工定制任务 | 自动支付回调 |
| `admin` | 管理用例聚合和操作审计 | 绕过其他模块规则直接改表 |

### 9.4 模块内分层

```text
controller       HTTP 入参、身份提取、响应转换
application      用例编排、事务、权限决策、跨模块调用
domain           状态、规则、值对象；不依赖 Web 和数据库
infrastructure   Mapper、对象存储、支付、短信等适配器
```

简单 CRUD 不强行制造复杂领域模型，但发布、支付、权益、删除等状态变化必须通过应用服务完成。

---

## 10. 技术基线

### 10.1 后端

| 项目 | 基线 |
|---|---|
| Java | JDK 21 LTS，Maven 编译目标设为 21 |
| Spring Boot | 4.1.x 稳定版，初始化时锁定确切补丁版本 |
| Web | Spring MVC + Jakarta Validation |
| 安全 | Spring Security + Spring Session JDBC |
| 数据 | PostgreSQL 17 + Spring JDBC + Flyway |
| API | OpenAPI 3.1，接口前缀 `/api/v1` |
| 测试 | JUnit 5、Spring Boot Test、Testcontainers、MockMvc |
| 构建 | Maven Wrapper，项目内固定 Maven 行为 |

截至 2026-09-04，Spring Boot 官方文档显示 4.1.1 支持 Java 17–26。首版数据访问采用 Spring JDBC：查询数量有限、SQL 需要清晰可审查；查询复杂度明显增加时，再以独立技术决策引入 MyBatis-Plus。初始化时必须再次查看稳定版，不使用 milestone 或 snapshot。

### 10.2 前端

| 项目 | 基线 |
|---|---|
| 框架 | Vue 3、Composition API、`<script setup lang="ts">` |
| 构建 | Vite，使用官方 `create-vue` 初始化 |
| 路由 | Vue Router |
| 状态 | Pinia；只存跨页面业务状态，局部表单不全部进入全局状态 |
| 请求 | 基于 `fetch` 封装统一客户端，携带 Cookie 和 CSRF Token |
| 样式 | CSS Variables + scoped CSS；设计令牌集中维护 |
| 表单 | 自建轻量表单规则或选择一个库后统一使用，禁止并存两套 |
| 单元测试 | Vitest + Vue Test Utils |
| 端到端 | Playwright |
| 质量 | ESLint、Prettier、vue-tsc |

Vue 官方推荐通过 `create-vue` 建立基于 Vite、支持 TypeScript 的项目；生产依赖通过 `package-lock.json` 固定。

当前实现已将全站视觉令牌、品牌/导航和套餐文案分别集中到 `web/src/assets/design-tokens.css`、`web/src/config/site.ts` 与 `web/src/config/pricing.ts`，便于在不触及业务流程的前提下调整视觉与常用内容。

### 10.3 基础设施

- Nginx：TLS 终止、静态文件、压缩、缓存与反向代理；
- PostgreSQL：主业务数据；
- 对象存储：照片、视频、导出包；
- CDN：公开派生图和静态资源；
- 短信提供器：接口抽象，开发环境使用本地实现；
- 支付提供器：接口抽象，开发环境使用模拟实现；
- 内容审核提供器：先规则与人工，预留第三方实现。

### 10.4 不引入的技术

- Redis：单实例 MVP 暂无必要；限流可先使用进程内方案，扩容前再迁移；
- Kafka/RabbitMQ：没有需要独立扩展的异步吞吐；
- Elasticsearch：PostgreSQL 检索足够；
- Kubernetes：单机 Docker Compose 或云主机足够；
- GraphQL：REST 更适合当前固定业务边界；
- 多仓库：增加版本同步成本。

---

## 11. 代码仓库结构

```text
ai-animal/
├─ web/
│  ├─ src/
│  │  ├─ api/                 生成或封装的 API 客户端
│  │  ├─ assets/              品牌与静态资源
│  │  ├─ components/          通用 UI 组件
│  │  ├─ composables/         可复用组合式逻辑
│  │  ├─ modules/
│  │  │  ├─ auth/
│  │  │  ├─ create/
│  │  │  ├─ editor/
│  │  │  ├─ memorial/
│  │  │  ├─ billing/
│  │  │  ├─ account/
│  │  │  └─ admin/
│  │  ├─ router/
│  │  ├─ stores/
│  │  ├─ styles/
│  │  └─ main.ts
│  ├─ tests/
│  ├─ package.json
│  └─ vite.config.ts
├─ server/
│  ├─ src/main/java/.../
│  │  ├─ shared/
│  │  ├─ user/
│  │  ├─ memorial/
│  │  ├─ media/
│  │  ├─ tribute/
│  │  ├─ billing/
│  │  ├─ moderation/
│  │  ├─ service/
│  │  └─ admin/
│  ├─ src/main/resources/
│  │  ├─ db/migration/
│  │  ├─ application.yml
│  │  └─ templates/memorial-shell.html
│  ├─ src/test/
│  ├─ pom.xml
│  └─ mvnw / mvnw.cmd
├─ deploy/
│  ├─ nginx/
│  ├─ docker-compose.local.yml
│  ├─ docker-compose.prod.yml
│  └─ .env.example
├─ docs/
│  ├─ api/openapi.yml
│  ├─ adr/
│  └─ runbooks/
├─ .editorconfig
├─ .gitignore
└─ README.md
```

---

## 12. 数据模型

所有业务主键使用 UUID；数据库时间使用 UTC，展示时转换为 Asia/Shanghai。金额使用整数分，不使用浮点数。

### 12.1 核心表

| 表 | 关键字段 | 约束与索引 |
|---|---|---|
| `users` | id、phone_hash、phone_encrypted、status、created_at | 手机摘要唯一；号码密文与检索摘要分离 |
| `user_roles` | user_id、role | 联合唯一 |
| `consent_records` | user_id、type、version、accepted_at、ip_digest | 保留用户同意的版本证据 |
| `pets` | id、owner_id、name、species、birth_precision、departed_precision | owner_id 索引 |
| `memorials` | id、owner_id、pet_id、slug、status、visibility、theme_key、version | slug 唯一；owner_id/status 索引；乐观锁 version |
| `memorial_settings` | memorial_id、music_media_id、guestbook_enabled、indexable、password_hash | memorial_id 唯一 |
| `timeline_entries` | id、memorial_id、date_value、date_precision、title、body、sort_order | memorial_id/sort_order 索引 |
| `letters` | id、memorial_id、author_label、body、sort_order | memorial_id 索引 |
| `media_assets` | id、owner_id、object_key、kind、mime、size、status、checksum | object_key 唯一；owner/status 索引 |
| `memorial_media` | memorial_id、media_id、section、caption、alt_text、sort_order | 联合唯一；排序索引 |
| `tributes` | id、memorial_id、type、visitor_name、body、status、created_at | memorial/status/time 索引 |
| `reports` | id、target_type、target_id、reason、status、reporter_fingerprint | status/time 索引 |
| `plans` | code、name、price_fen、rights_json、active | code 唯一 |
| `orders` | id、user_id、plan_code、amount_fen、status、idempotency_key、snapshot_json | idempotency_key 唯一；user/time 索引 |
| `payments` | id、order_id、provider、provider_trade_no、status、callback_hash | 外部交易号唯一 |
| `entitlements` | id、user_id、memorial_id、type、value_json、starts_at、ends_at | memorial/type 索引 |
| `service_jobs` | id、order_id、assignee_id、status、due_at、revision_count | status/due_at 索引 |
| `moderation_cases` | id、target_type、target_id、status、decision、operator_id | status/time 索引 |
| `audit_logs` | id、actor_id、action、target_type、target_id、reason、created_at | actor/time、target 索引 |
| `export_jobs` | id、user_id、status、object_key、expires_at | user/time 索引 |
| `deletion_requests` | id、user_id、status、execute_after、completed_at | status/execute_after 索引 |

### 12.2 状态机

纪念页：

```text
DRAFT → PUBLISHED → HIDDEN → PUBLISHED
  │          │          │
  └──────────┴──────────→ PENDING_DELETION → DELETED
```

订单：

```text
PENDING → PAID → FULFILLING → COMPLETED
   │        │          │
   └→ CLOSED└→ REFUNDING → REFUNDED
```

媒体：

```text
PENDING_UPLOAD → PROCESSING → ACTIVE
       │              │         │
       └→ EXPIRED     └→ REJECTED└→ PENDING_DELETE → DELETED
```

状态迁移只能通过应用服务执行，不允许前端直接传任意状态覆盖。

### 12.3 数据保留

- 未完成上传：24 小时后清理；
- 导出包：生成后 24 小时失效并删除；
- 普通访问日志：默认保留 30 天并去标识化；
- 待删除内容：7 天撤销期，期满后的后台清理任务执行硬删除；
- 订单与支付记录：按适用法律和财务要求保存，并与公开内容隔离；
- 审计日志：MVP 保留 180 天，正式上线前按合规意见复核。

---

## 13. API 设计

### 13.1 通用约定

- API 前缀：`/api/v1`；
- 请求与响应：JSON，文件使用对象存储直传；
- 时间：ISO 8601 UTC；
- 金额：整数分；
- 分页：`cursor` + `limit`，后台简单列表可使用 page/size；
- 写操作支持 `Idempotency-Key`；
- 错误不返回堆栈、SQL、手机号或对象存储密钥；
- OpenAPI 文件是接口契约，前后端修改接口必须同步修改。

统一错误结构：

```json
{
  "error": {
    "code": "MEMORIAL_NOT_FOUND",
    "message": "没有找到这座纪念页",
    "fieldErrors": [],
    "requestId": "01J..."
  }
}
```

### 13.2 身份接口

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/auth/codes` | 发送登录验证码 |
| POST | `/auth/login` | 验证码登录并创建会话 |
| POST | `/auth/logout` | 注销当前会话 |
| GET | `/auth/me` | 当前用户与角色 |
| GET | `/auth/csrf` | 获取 CSRF Token |

### 13.3 纪念页接口

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/memorials` | 创建草稿 |
| GET | `/memorials` | 获取自己的纪念页列表 |
| GET | `/memorials/{id}` | 获取所有者编辑数据 |
| PATCH | `/memorials/{id}` | 更新基础信息、主题和隐私 |
| POST | `/memorials/{id}/publish` | 发布 |
| POST | `/memorials/{id}/hide` | 隐藏 |
| DELETE | `/memorials/{id}` | 发起删除 |
| GET | `/public/memorials/{slug}` | 访客获取可公开展示的数据 |

时间线、纪念信和媒体关联使用纪念页子资源，例如：

- `/memorials/{id}/timeline-entries`；
- `/memorials/{id}/letters`；
- `/memorials/{id}/media`。

### 13.4 媒体接口

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/media/uploads` | 校验后签发临时上传信息 |
| POST | `/media/uploads/{id}/complete` | 通知后端验证文件 |
| DELETE | `/media/{id}` | 删除媒体 |
| GET | `/media/{id}/access` | 获取有权限的限时访问地址 |

### 13.5 留言接口

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/public/memorials/{slug}/tributes` | 分页读取公开留言 |
| POST | `/public/memorials/{slug}/tributes` | 留言或点亮 |
| POST | `/public/tributes/{id}/reports` | 举报 |
| DELETE | `/memorials/{id}/tributes/{tributeId}` | 所有者隐藏留言 |

### 13.6 订单接口

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/plans` | 当前有效套餐 |
| POST | `/orders` | 创建订单 |
| GET | `/orders/{id}` | 查询订单 |
| POST | `/orders/{id}/pay` | 创建支付参数 |
| POST | `/payments/wechat/notify` | 微信支付回调，不使用浏览器会话认证 |
| POST | `/orders/{id}/refunds` | 用户申请退款或后台发起退款 |

### 13.7 人工定制接口

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/custom-service-requests` | 登录用户提交定制服务申请，不产生扣款 |
| GET | `/custom-service-requests` | 登录用户查看自己的服务进度 |
| POST | `/custom-service-requests/{id}/confirm-delivery` | 用户确认已交付的服务 |
| GET | `/admin/custom-service-requests` | 运营查看服务申请，联系方式仅限角色账户 |
| PATCH | `/admin/custom-service-requests/{id}` | 运营更新范围、材料、负责人、截止日、修改轮次和进度，并记录原因 |

### 13.8 管理接口

全部使用 `/api/v1/admin/**`，需要相应角色并写审计日志。禁止通过普通用户接口传入 `isAdmin` 等字段提升权限。

---

## 14. 前端设计

### 14.1 模块边界

每个业务模块包含自己的页面、组件、类型、API 封装和测试。通用组件只在两个以上模块真实复用时上移到 `components`。

### 14.2 状态管理

Pinia 只保存：

- 当前用户与权限；
- 当前编辑纪念页摘要；
- 套餐权益；
- 跨步骤创建状态。

上传进度、弹窗开关和单个表单字段保留在组件或 composable 内，避免全局 store 变成杂物间。

### 14.3 API 客户端

- 统一添加 `credentials: 'include'`；
- 写请求统一添加 CSRF Token；
- 401 跳转登录并保留返回地址；
- 403 显示无权限，不伪装成网络错误；
- 409 用于版本冲突并提示刷新；
- 429 显示稍后重试时间；
- 5xx 显示 requestId，方便客服定位。

### 14.4 编辑器保存策略

- 用户输入后 2 秒防抖保存；
- 保存请求携带 `version`；
- 后端成功后返回新 version；
- 旧 version 更新返回 409；
- 离线时保留当前浏览器副本并显示离线状态；
- 恢复网络后先比较版本，禁止直接覆盖服务端新内容。

### 14.5 前端埋点

| 事件 | 触发 |
|---|---|
| `home_primary_cta_clicked` | 点击首页主按钮 |
| `first_preview_completed` | 完成首屏预览 |
| `login_completed` | 登录成功 |
| `media_upload_completed` | 单次媒体上传成功 |
| `memorial_previewed` | 查看完整预览 |
| `memorial_published` | 发布成功 |
| `share_clicked` | 点击分享 |
| `checkout_started` | 创建订单前 |
| `payment_succeeded` | 后端确认已支付 |
| `tribute_submitted` | 留言或点亮提交成功 |
| `export_requested` | 发起数据导出 |

事件属性不包含手机号、留言正文、宠物照片地址或纪念信正文。

---

## 15. 后端关键设计

### 15.1 会话而非浏览器长期 JWT

本项目采用同域部署，使用 Spring Security 服务端会话更简单。浏览器只持有不可由 JavaScript 读取的会话 Cookie，权限撤销和退出能立即生效。

后续如果出现原生 App 或第三方开放 API，再单独设计短期访问令牌，不提前为未发生的需求增加复杂度。

### 15.2 权限规则

- 所有数据库查询都基于当前用户和资源归属，不接受前端传 ownerId；
- 公共纪念页接口只返回允许访客看到的字段；
- 密码页面验证成功后发放限时访问凭证；
- 客服只可查询必要信息，不能查看原始纪念信；
- 审核员查看被举报内容需要工单理由；
- 超级管理员操作仍需审计，不设置“无记录”通道。

### 15.3 支付幂等

支付回调处理步骤：

1. 验证签名与平台证书；
2. 校验商户号、订单号、金额和币种；
3. 使用外部交易号与订单状态做幂等；
4. 在同一数据库事务中更新支付、订单和权益；
5. 保存回调摘要，不原样记录敏感报文；
6. 返回支付平台要求的成功响应。

### 15.4 对象存储

对象路径使用不可猜测标识：

```text
private/{ownerUuid}/{memorialUuid}/{mediaUuid}/original.ext
derived/{memorialUuid}/{mediaUuid}/{variant}.webp
exports/{userUuid}/{exportUuid}.zip
```

不得使用手机号、昵称、原始文件名作为对象路径。原始文件私有；公开展示使用派生图或短期签名地址。

### 15.5 分享元数据

`/m/{slug}` 已由 Java 返回最小 HTML 壳，动态写入标题、简介、封面、robots 和 Open Graph 元数据，然后加载 Vue 纪念页入口。前端构建将入口文件固定为 `/assets/app.js` 与 `/assets/app.css`，Nginx 将该路径代理至 Java；Vue 路由与前端代码不重复维护。

只有页面允许访问时才输出真实信息；密码页面和已隐藏页面使用通用标题，防止元数据泄露宠物昵称与封面。

---

## 16. 安全、隐私与合规

### 16.1 安全基线

- 全站 HTTPS，开启 HSTS；
- Cookie 设置 Secure、HttpOnly、SameSite；
- Cookie 会话下所有写操作启用 CSRF 防护；
- CSP 限制脚本、图片、媒体和连接来源；
- 服务端校验字段长度、枚举、文件签名和资源归属；
- 所有用户内容按文本渲染，不直接注入 HTML；
- 登录、验证码、留言、点亮、举报和上传接口限流；
- 对象存储凭证最小权限、短期有效；
- 配置和密钥只从环境变量或密钥服务读取；
- 日志禁止记录验证码、Cookie、支付密钥、完整手机号和用户正文；
- 依赖漏洞扫描进入 CI，严重漏洞阻止发布。

### 16.2 隐私基线

- 默认最小收集；
- 页面默认仅链接可见；
- 明确展示托管期限；
- 公开索引由用户主动开启；
- 提供查看、导出、更正和删除入口；
- 上传家庭合照时提示授权责任；
- 明确内容不会未经允许用于模型训练；
- AI 辅助功能上线前单独征得同意并按适用规则标识。

### 16.3 上线依赖

- 运营主体和域名；
- 中国大陆服务器对应的 ICP 备案；
- 有偿服务所需资质的专项复核；
- 微信商户号及对应产品权限；
- 隐私政策、用户协议、内容规范和退款规则；
- 举报、审核、申诉与数据请求处理流程。

本节是产品与技术风险控制，不构成正式法律意见。

---

## 17. 性能与可靠性

### 17.1 性能目标

| 项目 | 目标 |
|---|---:|
| 首页移动端 LCP | p75 ≤2.5 秒 |
| 页面布局偏移 CLS | ≤0.1 |
| 普通读 API | p95 ≤300ms，不含外部服务 |
| 普通写 API | p95 ≤500ms，不含上传和支付 |
| 纪念页首屏图片 | 根据设备输出合适尺寸，优先 WebP |
| Vue 首屏 JS | 按路由拆包，非编辑器页面不加载编辑器代码 |

### 17.2 可靠性目标

- MVP 单实例月可用性目标 99.5%；
- 数据库每日全量备份并开启增量或 WAL 归档；
- RPO 目标 24 小时，正式收费前提升到 1 小时；
- RTO 目标 4 小时；
- 每月至少执行一次恢复演练；
- 发布保留上一镜像和数据库向前兼容迁移；
- Flyway 迁移只前进，不在生产环境自动执行破坏性回滚。

### 17.3 降级策略

- 短信故障：已登录用户继续可用，未登录用户显示故障而非反复扣费重试；
- 支付故障：保留待支付订单，不重复创建；
- 对象存储故障：文本内容仍可查看，编辑器暂停上传；
- 审核服务故障：新留言进入待审核，不默认公开；
- CDN 故障：关键静态资源回源，原始私有文件不开放。

---

## 18. 测试方案

### 18.1 后端测试

- 单元测试：状态机、套餐限制、日期精度、权限规则；
- 应用服务测试：发布、隐藏、支付、退款、导出和删除；
- 数据库集成测试：本地快速 API 闭环可使用 H2；合并前仍需补充 Testcontainers PostgreSQL 测试，不以 H2 替代生产兼容性验证；
- Web 测试：身份、CSRF、参数校验、错误码和资源越权；
- 外部服务契约测试：短信、对象存储、支付模拟器；
- 迁移测试：全新数据库可从 V1 完整迁移。

### 18.2 前端测试

- Vitest：组件、composable、store 和表单规则；
- Vue Test Utils：上传状态、保存状态、套餐限制显示；
- Playwright：真实浏览器关键路径；
- 可访问性检查：焦点顺序、键盘操作、语义与对比度；
- 390px、768px、1440px 三档视口截图回归。

### 18.3 必须通过的 E2E

1. 新用户完成首屏预览、登录、上传三张图并发布；
2. 所有者修改已发布页面，访客刷新后看到更新；
3. 仅链接、密码和公开三种访问策略正确；
4. 访客留言，所有者隐藏，管理员可查看审计记录；
5. 模拟支付回调重复三次，只发放一次权益；
6. 普通用户尝试访问他人草稿、订单和媒体均失败；
7. 用户导出后能下载，过期地址失效；
8. 用户申请删除后访客立即无法访问，并可在撤销期恢复。

### 18.4 缺陷等级

| 等级 | 定义 | 上线要求 |
|---|---|---|
| P0 | 数据泄露、支付错误、内容丢失、全站不可用 | 必须为 0 |
| P1 | 核心流程无法完成、严重越权、主流设备不可用 | 必须为 0 |
| P2 | 有替代路径的功能错误、明显视觉问题 | 记录负责人和修复日期 |
| P3 | 轻微文案、间距和低频问题 | 可进入后续版本 |

---

## 19. 可观测性与运营数据

### 19.1 技术监控

- HTTP 请求量、延迟、4xx/5xx；
- JVM 堆、GC、线程和数据库连接池；
- 数据库慢查询和锁等待；
- 对象存储上传失败率；
- 短信成功率与单手机号发送量；
- 支付回调成功率、延迟和重复次数；
- 导出与删除任务积压；
- 磁盘、备份和证书到期。

### 19.2 产品漏斗

```text
首页访问
→ 点击创建
→ 完成首屏预览
→ 登录
→ 上传 3 张照片
→ 完整预览
→ 发布
→ 分享
→ 亲友访问
→ 付费
```

每周复盘每一步转化，优先修复最大流失点，不以总注册数代替有效使用。

### 19.3 告警

- 5xx 比例 5 分钟超过 2%；
- 支付回调连续失败；
- 数据库连接池使用率超过 85%；
- 对象存储上传失败率 10 分钟超过 5%；
- 备份任务失败；
- P0 安全事件立即通知负责人并关闭相关入口。

---

## 20. 部署方案

### 20.1 环境

| 环境 | 用途 | 数据 |
|---|---|---|
| local | 开发 | 优先 Docker PostgreSQL、MinIO、模拟短信和支付；Docker 不可用时可显式启用短生命周期 H2 配置做本机联通验收 |
| test | 自动化与联调 | 脱敏测试数据，按流水线重建 |
| staging | 上线前验收 | 与生产同结构，不使用真实用户数据 |
| production | 正式用户 | 受控访问、备份、监控和审计 |

### 20.2 构建产物

- 前端：`web/dist` 静态文件；
- 后端：可执行 Spring Boot JAR 或 OCI 镜像；
- 数据库：Flyway 迁移脚本；
- 部署：Nginx 配置、Compose 文件与环境变量清单；
- 文档：OpenAPI、运行手册、备份恢复和回滚手册。

### 20.3 发布步骤

1. CI 完成格式、静态检查、单测、集成测试、前端构建和 E2E；
2. 构建带不可变版本号的前后端镜像；
3. 备份生产数据库并验证备份存在；
4. 在 staging 执行数据库迁移和冒烟测试；
5. 生产先执行向前兼容迁移，再发布后端，最后发布前端；
6. 执行登录、创建、发布、留言和模拟小额支付检查；
7. 观察错误率、支付和数据库 30 分钟；
8. 记录发布版本、执行人和结果。

### 20.4 回滚原则

- 前端可回滚到上一份静态产物；
- 后端回滚到上一镜像；
- 数据库迁移必须向前兼容上一后端版本；
- 如果迁移包含不可逆删除，拆成“停止使用 → 观察 → 后续版本删除”三步；
- 支付异常时关闭新支付入口，但保留已支付查询和人工核对能力。

---

## 21. 内容运营与付费验证

开发与内容验证并行，不等网站全部完成后才找用户。

### 21.1 首批资产

- 猫、狗、小型宠物三套完整演示样板；
- 暖阳、星夜、花园三套主题预览；
- 5–10 条小红书/抖音图文或视频；
- 价格页与人工定制材料清单；
- 隐私、导出和删除说明；
- 一份真实报价与访谈记录表。

### 21.2 付费验证门槛

- 至少 10 个有效咨询；
- 至少 5 人愿意提交真实材料；
- 至少 2 个真实付费订单或 3 个可退订金订单；
- 用户首先复述的价值是“整理和保存回忆”，不是“AI 很新奇”。

### 21.3 内容比例

- 50% 真实陪伴故事；
- 30% 纪念页面成品与制作过程；
- 20% 照片整理、纪念信写作等实用内容。

没有授权的案例必须标记“演示内容”，不能伪装真实客户。

---

## 22. 团队职责

单人执行时仍使用角色视角检查，避免只完成代码。

| 角色 | 责任 |
|---|---|
| 产品负责人 | 范围、价格、用户访谈、指标和验收 |
| 设计负责人 | 页面结构、视觉令牌、主题、无障碍与视觉验收 |
| 前端负责人 | Vue 页面、编辑器、上传、状态和前端测试 |
| 后端负责人 | Java 业务、权限、支付、媒体、数据和后端测试 |
| 运维负责人 | 环境、部署、备份、监控和回滚 |
| 运营负责人 | 样板、内容、人工定制和用户支持 |

一个人兼任多个角色时，每个阶段仍需分别完成对应检查表。

---

## 23. 执行计划

### 23.1 Sprint 0：开工准备，1–2 天

任务：

- [ ] 安装并配置 JDK 21，确认 `java -version`；
- [ ] 修复 Docker 用户配置权限警告，确认 `docker compose version`；
- [ ] 确认 Maven、Node 和 npm；
- [ ] 创建目录、Git 仓库、README、环境变量示例；
- [ ] 确认工作名称、Logo 文字版、三套主题色板；
- [ ] 建立 OpenAPI、ADR 和 Flyway 目录；
- [ ] 确认首发短信、对象存储和支付使用真实还是模拟提供器。

验收：

- [ ] 新电脑根据 README 能启动 PostgreSQL、对象存储、Java 和 Vue；
- [ ] 前端 `/health` 页面能读取后端 `/api/v1/health`；
- [ ] 仓库中不存在真实密钥。

### 23.2 Sprint 1：工程底座，3–4 天

后端：

- [ ] 初始化 Spring Boot、模块包、统一响应和异常处理；
- [ ] 配置 PostgreSQL、MyBatis-Plus、Flyway；
- [ ] 建立用户、角色、同意和会话表；
- [ ] 实现本地验证码提供器、登录、退出和当前用户；
- [ ] 启用 CSRF、Cookie 安全和基础限流。

前端：

- [ ] 使用 `create-vue` 初始化 TypeScript、Router、Pinia、Vitest 和 Playwright；
- [ ] 建立设计令牌、页面壳、路由和 API 客户端；
- [ ] 完成首页、价格页、登录页静态结构；
- [ ] 建立格式化、类型检查和测试命令。

验收：

- [ ] 用户可通过本地验证码登录与退出；
- [ ] 未登录访问受保护接口返回 401；
- [ ] CSRF 测试通过；
- [ ] 前后端测试在一条命令中运行。

### 23.3 Sprint 2：创建与草稿，4–5 天

- [ ] 建立 pets、memorials、settings、timeline、letters 表；
- [ ] 完成纪念页创建、读取、更新与乐观锁；
- [ ] 完成创建向导和首屏预览；
- [x] 完成桌面三栏／移动编辑与预览标签编辑器骨架；
- [ ] 实现自动保存、失败重试和版本冲突；
- [x] 完成主题令牌切换。

验收：

- [ ] 新用户完成登录后可创建并重新打开草稿；
- [ ] 两个浏览器同时修改时旧版本不能覆盖新版本；
- [x] 移动端完成创建流程无横向滚动；
- [ ] 刷新页面不丢失已成功保存内容。

### 23.4 Sprint 3：媒体与内容编辑，4–5 天

- [ ] 建立对象存储本地模拟、媒体表和上传状态机；
- [ ] 实现上传签名、直传、完成确认和访问授权；
- [ ] 实现照片选择、批量上传、进度、重试和排序；
- [x] 实现时间线、照片说明和纪念信编辑；
- [ ] 实现套餐容量校验；
- [ ] 生成封面缩略图和展示派生图。

验收：

- [ ] 10 张照片并行上传不会产生重复记录；
- [ ] 其他用户无法访问私有媒体；
- [ ] 删除图片后页面立即更新，后台进入延迟删除；
- [ ] 上传失败不影响已经成功的文件。

### 23.5 Sprint 4：发布、分享与访客互动，4–5 天

- [ ] 实现发布检查、slug、三种隐私模式；
- [x] 实现 `/m/{slug}` Java 动态元数据壳和 Vue 纪念页；
- [x] 完成三套主题的正式纪念页；
- [ ] 实现留言、点亮、举报、限流和所有者隐藏；
- [x] 实现分享卡片与复制链接。
- [ ] 实现页面隐藏和恢复。

验收：

- [ ] 三种隐私模式的 E2E 全部通过；
- [x] 分享预览不会泄露密码页面信息；
- [ ] 音乐默认不播放；
- [ ] 留言 XSS、刷接口和越权测试通过。

### 23.6 Sprint 5：订单、权益与后台，4–5 天

- [ ] 建立套餐、订单、支付、权益和退款模型；
- [ ] 实现模拟支付全流程；
- [ ] 对接正式支付前完成验签、幂等和金额校验测试；
- [ ] 实现套餐权益在编辑器和后端同时校验；
- [x] 完成管理后台的公开页巡查/下线、留言审核、举报队列、订单概览与审计记录功能；
- [x] 完成人工定制工单最小流程。

验收：

- [ ] 重复支付回调只发一次权益；
- [ ] 篡改前端价格无法少付；
- [x] 管理员操作全部进入审计日志；
- [x] 普通用户无法调用管理接口。

### 23.7 Sprint 6：隐私、质量与上线，4–5 天

- [x] 实现数据导出、账号删除请求和撤销期；
- [x] 提供隐私政策、协议、内容规范、退款说明页面（生产文案仍须由实际运营主体复核）；
- [x] 完成基础性能、无障碍、安全与响应式自动化检查；
- [ ] 配置 staging、生产环境、TLS、备份、监控和告警；
- [ ] 完成 E2E、恢复演练和回滚演练；
- [ ] 用 10 名种子用户做任务测试并修复 P0/P1。

验收：

- [ ] 第 18.3 节全部 E2E 通过；
- [ ] P0/P1 为 0；
- [ ] 备份可以恢复；
- [ ] 导出包完整，删除流程可验证；
- [ ] 上线检查表全部完成。

---

## 24. 每日执行节奏

建议每天按以下顺序工作：

1. 选择当天唯一主要交付结果；
2. 先写或更新验收测试；
3. 实现最小闭环；
4. 运行相关单测、集成测试和 E2E；
5. 自查移动端与错误状态；
6. 更新任务勾选、决策和风险；
7. 提交一个目的清楚、可回滚的变更。

每个 Sprint 结束时输出：

- 完成与未完成任务；
- 可演示的用户路径；
- 测试结果；
- 新增风险；
- 下一阶段是否放行。

---

## 25. 完成定义

一个功能只有同时满足以下条件，才算完成：

- [ ] 用户行为和边界符合验收标准；
- [ ] 服务端权限与参数校验完成；
- [ ] 正常、空、加载、错误和无权限状态完整；
- [ ] 移动端和桌面端可用；
- [ ] 单测或集成测试覆盖核心规则；
- [ ] 关键路径 E2E 更新；
- [ ] 不在日志或埋点泄露用户内容；
- [ ] OpenAPI 与代码一致；
- [ ] 监控或审计能够定位问题；
- [ ] 文档同步更新；
- [ ] 没有引入未记录的新范围。

这叫“完成定义（Definition of Done）”：不是代码能运行就算完成，而是产品、测试、安全和文档都达到可交付状态。

---

## 26. 上线门禁

### 26.1 产品门禁

- [x] 3 个完整样板可访问（均明确标注为演示内容）；
- [ ] 免费创建、发布、分享和留言流程完整；
- [ ] 套餐价格、容量、托管期限和退款边界一致；
- [ ] 至少 10 名种子用户完成任务测试；
- [ ] 客服和人工定制交付人明确。

### 26.2 技术门禁

- [ ] P0/P1 缺陷为 0；
- [ ] 越权、XSS、CSRF、上传和支付测试通过；
- [ ] 数据库备份、恢复和发布回滚演练完成；
- [ ] 日志、监控和 requestId 可用；
- [ ] 正式环境没有测试账号、模拟支付入口和默认密码；
- [ ] 密钥未进入 Git 历史。

### 26.3 合规门禁

- [ ] 主体、域名和备案满足上线地区要求；
- [ ] 正式支付权限已通过或支付入口保持关闭；
- [ ] 用户协议、隐私政策、内容规范和退款规则上线；
- [ ] 数据导出、删除、举报和申诉入口可用；
- [ ] 公开案例均有授权或明确标记为演示。

---

## 27. 风险清单

| 风险 | 概率 | 影响 | 开工时的应对 |
|---|---|---|---|
| 功能做完但没人付费 | 高 | 高 | 内容与真实报价和开发并行，每周检查咨询和订单 |
| 编辑器范围膨胀 | 高 | 高 | MVP 固定六个内容模块和三套主题，不做自由页面搭建器 |
| 视频存储与流量超预算 | 中 | 高 | 严格容量、图片优先、视频总额限制和派生文件 |
| 用户隐私泄露 | 中 | 极高 | 默认非公开、私有存储、资源级权限和越权测试 |
| 支付状态错乱 | 中 | 极高 | 服务端回调、金额核对、事务和幂等 |
| 留言出现违规内容 | 中 | 高 | 限流、规则、待审核、举报和后台处置 |
| “永久保存”承诺无法兑现 | 中 | 高 | 明确 3/10 年托管、续期、导出和停运预案 |
| Java/Vue 两套工程增加联调 | 高 | 中 | OpenAPI 契约、同仓库、同域部署、统一错误结构 |
| 主体或支付审核拖延 | 中 | 高 | 模拟支付不阻塞开发，正式入口未获批前不开放 |
| 单人兼任导致质量遗漏 | 高 | 中 | 每 Sprint 按产品、设计、开发、测试、运维分别检查 |

---

## 28. 当前环境审计

2026-09-04 在本机检查结果：

| 工具 | 当前状态 | 开工动作 |
|---|---|---|
| Java | 22.0.1 | 安装或配置 JDK 21，并让 Maven/IDE 使用同一版本 |
| Maven | 3.9.9 | 可用；项目仍提交 Maven Wrapper |
| Node.js | 24.18.0 | 满足当前 Vue 官方脚手架要求 |
| npm | 11.16.0 | 可用；使用 package-lock 固定依赖 |
| Docker | 29.7.2 | 命令可用，但读取用户配置文件时出现权限警告，Sprint 0 修复 |
| Git | 当前目录不是 Git 仓库 | Sprint 0 初始化，提交现有文档作为首个基线 |

---

## 29. 开工时的第一批任务

完成本文档确认后，严格按以下顺序开始：

1. 初始化 Git，并提交三份 Markdown 基线文档；
2. 修复 JDK 与 Docker 环境；
3. 创建 `web`、`server`、`deploy`、`docs` 目录；
4. 初始化 Vue 3 + TypeScript + Router + Pinia + Vitest + Playwright；
5. 初始化 Java 21 + Spring Boot + Security + Validation + PostgreSQL + Flyway；
6. 建立本地 PostgreSQL、MinIO 和模拟短信/支付；
7. 完成 `/api/v1/health` 和 Vue 健康检查页面；
8. 建立统一测试命令与 README；
9. 通过 Sprint 0 验收后进入登录和工程底座。

禁止在完成上述底座之前开发 AI、实体商品、社交广场或复杂主题编辑器。

---

## 30. 决策记录

### ADR-001：使用 Java + Vue 前后端分离

**状态：已接受**

选择 Vue 负责交互与页面，Spring Boot 负责业务、权限、支付与数据。代价是两套构建和 API 联调；收益是符合团队技术偏好，并允许编辑器与业务后端独立演进。

### ADR-002：后端采用模块化单体

**状态：已接受**

早期没有微服务带来的独立扩容和团队自治需求。单体能保留本地事务和简单部署；通过模块边界避免演变成无结构代码。

### ADR-003：使用服务端会话 Cookie

**状态：已接受**

系统同域部署且主要服务浏览器。服务端会话比浏览器长期 JWT 更容易撤销，也减少令牌泄露面；未来出现 App 或开放 API 时再增加令牌方案。

### ADR-004：默认仅链接可见

**状态：已接受**

纪念内容天然敏感，不能以增长为理由默认公开。用户需要主动开启搜索引擎索引；密码页面的元数据也不得泄露真实内容。

### ADR-005：先模拟支付，再接真实支付

**状态：已接受**

订单、回调、幂等和权益可以先完整开发测试，避免商户审核阻塞工程；正式环境只有在主体与支付权限通过后才启用真实入口。

---

## 31. 参考资料

技术：

- [Spring Boot 系统要求](https://docs.spring.io/spring-boot/system-requirements.html)
- [Spring Security 会话管理](https://docs.spring.io/spring-security/reference/7.0/servlet/authentication/session-management.html)
- [Vue 官方快速开始](https://vuejs.org/guide/quick-start)
- [Vue + TypeScript](https://vuejs.org/guide/typescript/overview)
- [Vite 官方指南](https://vite.dev/guide/)

合规与支付：

- [《中华人民共和国个人信息保护法》—中国人大网](https://www.npc.gov.cn/WZWSREL25wYy9jMzA4MzQvMjAyMTA4L3QyMDIxMDgyMF8zMTMwODguaHRtbD9yZWY9aW1i)
- [《非经营性互联网信息服务备案管理办法》—工业和信息化部](https://www.miit.gov.cn/gyhxxhb/jgsj/cyzcyfgs/bmgz/xxtxl/art/2024/art_84a0cfa0ebd049bbbe751dca9a008e56.html)
- [《人工智能生成合成内容标识办法》发布说明—中国网信网](https://www.cac.gov.cn/2025-03/14/c_1743654685899683.htm)
- [微信支付 H5 支付接入准备](https://pay.wechatpay.cn/doc/v3/merchant/4015614193)

---

## 32. 最终执行结论

项目按照“Vue 负责体验、Java 负责业务、同域部署、模块化单体、默认私密”的方式建设。第一阶段的成功标准不是功能数量，而是一个用户能够安全、顺畅地创建并分享完整纪念页，平台能够正确处理隐私、媒体、留言、支付和删除。

执行时优先保障以下五件事：

1. 创建过程不丢内容；
2. 私密内容不越权；
3. 图片上传和展示稳定；
4. 支付和权益绝不重复或错发；
5. 用户随时可以导出和删除自己的内容。

只要这五项成立，项目就具备公测基础；其他增强功能全部排在其后。
